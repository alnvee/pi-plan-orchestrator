{
  "name": "pi-plan-orchestrator",
  "version": "0.0.1",
  "license": "MIT",
  "description": "UI-first plan orchestration extension for Pi powered by pi-subagents.",
  "type": "module",
  "private": false,
  "scripts": {
    "build": "echo 'nothing to build'",
    "check": "npm test && npx tsc --noEmit",
    "test": "node --experimental-strip-types --test test/**/*.test.ts"
  },
  "keywords": ["pi-package", "pi", "subagents", "orchestrator"],
  "files": ["src", "README.md", "LICENSE"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "dependencies": {
    "pi-subagents": "^0.25.0",
    "typebox": "^1.1.24",
    "yaml": "^2.9.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
